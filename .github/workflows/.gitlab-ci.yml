# Простой пайплайн для запуска тестов
stages:
  - test

run_tests:
  stage: test
  image: python:3.9
  before_script:
    - apt-get update
    - pip install -r requirements.txt
  script:
    - python -m pytest tests/ -v
  only:
    - main
